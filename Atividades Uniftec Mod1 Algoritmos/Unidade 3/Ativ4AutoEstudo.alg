algoritmo "Ativ4Autoavaliacao"
var
   x, y : real
   vz : inteiro
inicio
   leia(x)
   y <- 0
   para vz de 1 ate 10 faca
        y <- (vz*x)+y
        escreval(y)
   fimpara
   escreval(y)
fimalgoritmo

algoritmo "calculo area do triangulo"
var
 base, altura, area : real
inicio
      escreva("Insira a altura: ")
      leia(altura)
      escreva("Insira a base: ")
      leia(base)
      area <- (base*altura)/2
      escreva(area)
fimalgoritmo